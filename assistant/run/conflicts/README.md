# Assistant clue conflicts

Rejected differing observations are written here for manual review. When an
assistant file has top-level `"auto-update": "false"`, runtime findings and
verification attempts are also written here with `locked-file-finding` or
`locked-file-verification` reasons. Conflict files are never merged into an
active assistant automatically.
