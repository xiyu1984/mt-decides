Promotion form policy

- Stay on the configured origin unless "推广通" or "新建推广" uses a normal same-business redirect required by the workflow.
- Locate controls by their current visible labels and surrounding section context. Do not rely on remembered coordinates.
- "推广通" is expected in the top bar of the right-side component; verify its location from the current UI before clicking.
- "新建推广", "标准推", and "立即创建" are distinct controls. Re-check the visible label before every action involving them.
- Treat the promotion settings page as a draft form. Filling fields is allowed; activating "立即创建" is not.
